@page
@model DrinkOptions.Pages.Drinks.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Drink</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Drink.STea)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Drink.STea)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Dink.UTea)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Drink.UTea)
        </dd>
        <ul>       
                @* 
                *@
                @foreach(var course in Model.Drink.DrinkOptions)
                {
                    <form method="post">
                        <input type="hidden" asp-for="OptionIDToDelete" value="@option.option.OptionID" />
                        <li>
                            @option.Option.Description <input type="submit" Value="Drop" class="btn btn-danger btn-sm my-1" asp-page-handler="OptionDelete" asp-route-id="@Model.Drink.DrinkID">
                        </li>
                    </form>
                }
        </ul>
    </dl>
    <div class="row">
    <div class="col-md-4">
        <form method="post">
            <input type="hidden" asp-for="Drink.DrinkID" />
            <div class="form-group">
                <select asp-items="Model.OptionsDropDown" asp-for="OptionIdToAdd" class="form-control">
                    <option value="">--Select Option to Add--</option>
                </select>
                <span asp-validation-for="OptionIdToAdd" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add Option" class="btn btn-primary" asp-page-handler="" asp-route-id="@Model.Drink.DrinkID"/>
            </div>
        </form>
    </div>
    </div>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Student.StudentID">Edit</a> |
    <a asp-page="./Index">Back to Drink</a>
</div>